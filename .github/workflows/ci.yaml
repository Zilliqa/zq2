name: ZQ2 CI

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: sudo apt install -y protobuf-compiler
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    # Use Rust nightly so we can use the Cargo sparse registry feature, which greatly speeds up uncached builds (https://blog.rust-lang.org/2022/06/22/sparse-registry-testing.html).
    - name: Use Rust nightly
      run: rustup override set nightly && rustup component add clippy rustfmt
    - uses: Swatinem/rust-cache@v2
    - name: rustfmt
      run: cargo fmt --all --check
    - name: Build
      run: cargo build --all-targets --all-features
    - name: Clippy
      run: cargo clippy --all-targets --all-features -- -D warnings
    - name: Test
      run: cargo test --all-targets --all-features
        #- name: JS test framework
        #  run: ./scripts/js_test.sh

  e2e_test:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: sudo apt install -y protobuf-compiler
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    # Use Rust nightly so we can use the Cargo sparse registry feature, which greatly speeds up uncached builds (https://blog.rust-lang.org/2022/06/22/sparse-registry-testing.html).
    - name: Use Rust nightly
      run: rustup override set nightly && rustup component add clippy rustfmt
    - uses: Swatinem/rust-cache@v2
    - name: lsall
      run: docker create --name dummy zilliqa/zilliqa:v9.0.4 && docker cp dummy:/ /.
        #- name: lsall2
        #run: docker pull zilliqa/zilliqa:v9.0.4 && docker run -it -d --entrypoint /bin/bash zilliqa/zilliqa:v9.0.4 && docker ps -aqf "name=^zilliqa$" && docker ps
        #- name: thing
      #run: AA=docker ps -aqf "name=^zilliqa$";echo "is $AA"
        # && docker exec -it `docker ps -aqf "name=^zilliqa$"` echo "Hello from container!"
        #- name: Do
        #run: docker cp zilliqa/zilliqa:v9.0.4:/ ./
        #run: docker cp -r zilliqa/zilliqa:v9.0.4:/ ./; docker cp zilliqa/zilliqa:v9.0.4:/scilla /.; docker cp zilliqa/zilliqa:v9.0.4:/scilla/0/bin/scilla-checker /.; docker cp zilliqa/zilliqa:v9.0.4:/scilla/0/bin/scilla-server /.;
    - name: lsall
      run: ls
    - name: lsall
      run: find ./ -name scilla*
    - name: Build
      run: cargo build --all-targets --all-features
