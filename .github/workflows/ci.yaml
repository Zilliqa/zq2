name: ZQ2 CI

on:
  pull_request:
    types:
    - opened
    - edited
    - closed
    - reopened
    branches:
      - 'main'
  push:

jobs:
  zq2:
    uses: Zilliqa/gh-actions-workflows/.github/workflows/ci-rust-app.yml@v1
    with:
      dependencies: "protobuf-compiler"
