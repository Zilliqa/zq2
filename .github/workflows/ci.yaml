name: ZQ2 CI

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: sudo apt install -y protobuf-compiler
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    # Use Rust nightly so we can use the Cargo sparse registry feature, which greatly speeds up uncached builds (https://blog.rust-lang.org/2022/06/22/sparse-registry-testing.html).
    - name: Use Rust nightly
      run: rustup override set nightly && rustup component add clippy rustfmt
    - uses: Swatinem/rust-cache@v2
    - name: Build
      run: cargo build --all-targets --all-features
    - name: rustfmt
      run: cargo fmt --all --check
    - name: Clippy
      run: cargo clippy --all-targets --all-features -- -D warnings
    - name: Test
      run: cargo test --all-targets --all-features
    - name: Verify working directory is clean
      run: git diff --exit-code
    - name: Run a security audit
      run: cargo audit --no-fetch --ignore RUSTSEC-2021-0079 --ignore RUSTSEC-2022-0004 --ignore RUSTSEC-2020-0071 --ignore RUSTSEC-2022-0035 --ignore RUSTSEC-2021-0078
